apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microware-ingress
  labels:
    name: microware-ingress
spec:
  ingressClassName: nginx
  defaultBackend:
    service:
      name: error
      port:
        number: 80
  rules:
  - host: localhost
    http:
      paths:
        - path: /
          pathType: Prefix
          backend: 
            service:
              name: microcraft-api-service
              port:
                number: 80